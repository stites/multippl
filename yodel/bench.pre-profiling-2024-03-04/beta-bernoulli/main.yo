sample {
  pb ~ beta(1.0, 1.0);
  _ <- exact {
    let b1 = flip pb in observe b1 == xs1 in
    let b2 = flip pb in observe b2 == xs2 in
    let b3 = flip pb in observe b3 == xs3 in
    let b4 = flip pb in observe b4 == xs4 in
    let b5 = flip pb in observe b5 == xs5 in
    true
  };
  pb
}
