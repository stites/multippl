sample fn network(params: Float, obs: Bool ) -> (Float, Float) {
  A <- exact {
    let distA = flip params in
    observe obs in
    distA
  };
  modA <- if A then 1.0 else 2.0;
  (params, modA)
}

sample {
  params <- 0.5;
  network(params, false)
}
