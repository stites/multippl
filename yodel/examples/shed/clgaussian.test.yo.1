sample {
    p ~ beta(1.0, 1.0);
    ds ~ dirichlet(1.0, 1.0, 1.0);
    res <- exact {
       let fA1 = flip p in observe fA1 == false in
       let dB1 = discrete(ds[0], ds[1], ds[2]) in observe dB1 == 1 in
       true
    };
    (p, ds)
}
