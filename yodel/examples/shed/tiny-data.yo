sample {
  pb ~ beta(1.0, 1.0);
  pn ~ normal(0.0, 1.0);
  exact_b <- exact {
    let b = flip pb in
    observe (b == data_b) in
    b
  };
  b_skew <- if exact_b then 0.0 else 5.0;
  n <- normal(pn + b_skew, 1.0);
  observe data_n in n;
  (pb, pn)
}
